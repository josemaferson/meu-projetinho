CREATE DATABASE escola;

-- Conectar no banco escola antes de rodar o resto
-- \c escola

-- Criar tabela de alunos
CREATE TABLE alunos (
    id SERIAL PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    curso VARCHAR(100) NOT NULL,
    idade INT NOT NULL
);

-- Inserir dados
INSERT INTO alunos (nome, curso, idade)
VALUES
('João Silva', 'Informática', 19),
('Maria Souza', 'Administração', 20);

-- Consultar dados
SELECT * FROM alunos;